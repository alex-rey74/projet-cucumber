const calculator = {
    add(operand1, operand2 ){
        return operand1 + operand2
    },
    
    substract(operand1, operand2){
        return operand1 - operand2
    }
}

module.exports = calculator